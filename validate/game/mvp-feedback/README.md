# MVP feedback gate

The spectator-menu rule regression fails on the prior source. The installed
offline gate is recorded in validation.txt; source.json pins the checked files.

Visual and input acceptance is pending the [creator playtest](../../../docs/MVP-PLAYTEST.md).
No new browser run is claimed for these changes.
