# Player damage sound source

`damage.wav` is generated locally by `generate_sound.py`. It combines a short
decaying low-frequency thump with deterministic filtered noise. It does not use
external or Counter-Strike audio.
